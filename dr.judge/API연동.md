# Dr.Judge — API 연동 메모

> ## ⚠️ 2026-08-20 정정
>
> 배포 서버가 **`https://13-124-27-143.sslip.io/v3/api-docs`** 로 실제 스펙을
> 공개하고 있습니다. 아래 문서 중 일부가 실제 응답과 달라서, 그대로 읽었더니
> 화면이 비어 보이는 문제가 있었습니다. 고친 곳:
>
> | 화면 | 문서에 적혀 있던 이름 | 서버가 실제로 주는 이름 |
> |---|---|---|
> | 내 정보 조회 | `interestCategories` | **`interestCategoryCodes`** |
> | 공유 링크 공개 조회 | `extractedText` | **`title`** |
> | 공유 링크 공개 조회 | `conflictOfInterest.detected` | **`conflictDetected`** (평평함) |
> | 공유 링크 공개 조회 | `guideCard` (객체) | **`guideCardJson`** (JSON 문자열) |
> | 브리핑 | `briefingId`·`title`·`summary`·`trustLevelLabel` | **`categoryCode`·`trustLevel`·`target`·`effect`·`evidenceSummary`** |
>
> **명세가 헷갈릴 때는 이 문서보다 `/v3/api-docs` 를 믿으세요.**


## 서버 주소 · CORS

프론트는 Live Server(**5500**), 백엔드는 **8080** 이라 포트가 다릅니다.
`api.js` 가 주소를 알아서 만들기 때문에 평소엔 손댈 필요가 없습니다.

```js
const SERVER_URL = '';   // 비워두면 http://<현재 호스트>:8080/api
```

배포할 때만 여기에 실제 서버 주소를 적으면 됩니다.

### 백엔드에서 CORS 를 열어주세요

포트가 다르면 브라우저가 요청을 막습니다. Postman 은 브라우저가 아니라서 잘 되던 것이,
화면에서 부르면 "네트워크 연결을 확인해 주세요."로 보일 수 있습니다.

Spring Boot 라면 설정 클래스 하나만 추가하면 됩니다.

```java
@Configuration
public class CorsConfig implements WebMvcConfigurer {
    @Override
    public void addCorsMappings(CorsRegistry registry) {
        registry.addMapping("/api/**")
                .allowedOrigins(
                        "http://localhost:5500",
                        "http://127.0.0.1:5500")
                .allowedMethods("GET", "POST", "PATCH", "PUT", "DELETE", "OPTIONS")
                .allowedHeaders("*")
                .allowCredentials(true);
    }
}
```

> 브라우저 콘솔에 `blocked by CORS policy` 가 보이면 이 설정이 빠진 것입니다.
> `Access-Control-Allow-Origin` 응답 헤더가 오는지 확인하세요.

---

## 켜는 방법

`api.js` 위쪽 `LIVE` 에서, 서버에 만들어진 기능만 `true` 로 바꾸면 됩니다.

```js
const LIVE = {
  signup: true,          // 연동 완료
  login: true,           // 연동 완료
  checkDuplicate: false, // 명세 대기
  judge: true,           // 연동 완료
  onboarding: true,      // 연동 완료
  briefing: true,        // 연동 완료
  share: true,           // 연동 완료
  feed: true,            // 연동 완료
};
```

`false` 인 기능은 서버 없이 브라우저 안에서 그대로 동작하므로, 하나씩 켜면서 진행할 수 있습니다.
화면 코드는 전부 `API.xxx()` 와 `Store` 만 보고 있어서, 서버를 붙일 때 화면은 손대지 않습니다.

> 지금은 **인증 5종과 판정 3종**이 실서버에 연결돼 있습니다. 서버가 켜져 있어야 가입이 됩니다
> (`localhost:8080`). 서버가 꺼져 있으면 "네트워크 연결을 확인해 주세요."가 뜹니다.

---

## 서버 공통 응답 형태

```json
성공  { "success": true,  "data": { ... }, "error": null }
실패  { "success": false, "data": null,    "error": { ... } }
```

`api.js` 의 `request()` 가 이 껍데기를 벗겨서 화면에는 `data` 만 넘깁니다.
실패하면 `{ ok:false, code, field, text }` 로 바꿔서, 오류 문구가 해당 입력 칸 아래에 자동으로 붙습니다.

---

## 회원가입 (연동 완료)

`POST /api/auth/signup`

요청

| 파라미터 | 타입 | 설명 |
|---|---|---|
| loginId | String | 로그인 아이디 |
| password | String | 비밀번호 |
| email | String | 이메일 |
| name | String | 이름 |
| nickname | String | 닉네임 |

응답 `201 Created`

```json
{ "success": true, "data": { "userId": 5 }, "error": null }
```

| 상태코드 | 화면 동작 |
|---|---|
| 201 | 다음 단계(성별·연령대)로 이동 |
| 400 | "입력한 내용을 다시 확인해 주세요." |
| 409 | 이메일 칸에 "이미 가입된 이메일 주소예요." |
| 500 | "서버에 문제가 생겼어요. 잠시 후 다시 시도해 주세요." |

> 응답에 토큰이 없습니다. 로그인 API 가 준비되면 가입 직후 자동으로 한 번 더 호출해 토큰을 받도록
> 해뒀고(`LIVE.login = true` 로 켜면 동작), 그 전까지는 로컬 세션으로 다음 단계를 진행합니다.
> 서버가 준 `userId` 는 `profile.serverId` 에 보관합니다.

---

## 로그인 (연동 완료)

`POST /api/auth/login`

요청

| 파라미터 | 타입 | 설명 |
|---|---|---|
| loginId | String | 로그인 아이디 |
| password | String | 비밀번호 |

응답 `200 OK`

```json
{ "success": true, "data": { "accessToken": "...", "refreshToken": "..." }, "error": null }
```

| 상태코드 | 화면 동작 |
|---|---|
| 200 | 토큰 보관 후 홈으로 이동 |
| 400 | 아이디·비밀번호 **두 칸 모두** 빨간 표시 + "아이디 또는 비밀번호를 다시 확인해 주세요." |
| 409 | "이미 탈퇴한 계정이에요." |
| 500 | "서버에 문제가 생겼어요. 잠시 후 다시 시도해 주세요." |

> 400 이 아이디 오류와 비밀번호 오류를 구분하지 않아서, 명세서(3-1 / 4-1)처럼 칸을 나눠 표시하지
> 못하고 두 칸을 함께 표시합니다. 서버에서 구분해 주시면(예: 404 = 아이디 없음, 401 = 비밀번호 불일치)
> `api.js` 의 `statusMap` 한 줄만 고쳐서 칸별로 나눌 수 있습니다.

> 응답에 사용자 정보가 없어서, 프로필(닉네임·이메일 등)은 가입할 때 저장해 둔 값을 씁니다.
> `GET /users/me` 가 생기면 로그인 직후 `Store.hydrate()` 로 서버 값을 덮어쓰면 됩니다.

---

## 카카오 로그인 (연동 완료)

`POST /api/auth/kakao` — 요청 `{ code, redirectUri }`

**응답이 두 갈래입니다.** `token` 이 있으면 로그인 끝, 없으면 온보딩부터 받아야 합니다.

| | token | onboardingToken | 다음 화면 |
|---|---|---|---|
| 기존 회원 | 있음 | null | 홈 |
| 신규 회원 | **null** | 발급 (10분) | 온보딩 → `/auth/kakao/complete` |

| 상태코드 | 화면 동작 |
|---|---|
| 200 | 위 두 갈래로 나뉨 |
| 401 | "카카오 로그인이 취소됐거나 시간이 지났어요." |
| 409 | "이미 탈퇴한 계정이에요." |
| 500 | "서버에 문제가 생겼어요." |

### 버튼이 있는 곳

| 화면 | 문구 |
|---|---|
| `start.html` | 카카오로 시작하기 |
| `login.html` | 카카오로 시작하기 |
| `signup.html` (1단계) | 카카오로 가입하기 |

회원가입 1단계 안에 넣어서, **온보딩만 받는 모드(`?mode=onboarding`)에서는 자동으로 안 보입니다.**
이미 카카오로 들어온 사람에게 카카오 버튼을 또 보여줄 이유가 없으니까요.

### 전체 흐름

```
시작/로그인 화면  [카카오로 시작하기]
   ↓  kauth.kakao.com 으로 이동, 사용자가 동의
kakao-callback.html?code=...
   ↓  POST /auth/kakao { code, redirectUri }
   ├─ 기존 회원 → 토큰 저장 → home.html
   └─ 신규 회원 → onboardingToken 저장 → signup.html?mode=onboarding
                    ↓  성별·연령대 → 관심분야
                  POST /me/onboarding      (onboardingToken 실어서)
                    ↓
                  POST /auth/kakao/complete (onboardingToken 다시)
                    ↓  이번엔 token 이 옴 → welcome.html
```

### 넣어야 할 값 하나

`api.js` 맨 위의 **`KAKAO_CLIENT_ID`** 에 카카오 REST API 키를 넣어야 동작합니다.
(카카오 개발자센터 → 내 애플리케이션 → 앱 키 → REST API 키)

비어 있으면 버튼을 눌렀을 때 "키를 넣어달라"는 안내와 **등록해야 할 Redirect URI** 를 보여 줍니다.
버튼을 숨기지 않는 이유는, 없으면 왜 안 되는지 알 수 없기 때문입니다.

Redirect URI 는 지금 열려 있는 주소에서 자동으로 만듭니다.

| 여는 방법 | 만들어지는 Redirect URI |
|---|---|
| `node serve.js` (3000) | `http://localhost:3000/kakao-callback.html` |
| Live Server (5500) | `http://localhost:5500/dr.judge/kakao-callback.html` |

**카카오에 등록한 값과 글자 하나까지 같아야 합니다.** 다르면 카카오가 `KOE006` 을 돌려줍니다.
고정하고 싶으면 `api.js` 의 `KAKAO_REDIRECT_URI` 에 직접 적으면 됩니다.

### 두 가지 주의점

1. **액세스 토큰 이름이 `token` 입니다.** 아이디 로그인(1.2)은 `accessToken` 인데 카카오만 다릅니다.
   `api.js` 안에서 맞춰 두었으니 화면에서는 신경 쓰지 않아도 됩니다.

2. **`onboardingToken` 은 온보딩 저장 후에도 지우지 않습니다.**
   2.1 에 한 번, `/auth/kakao/complete` 에 한 번 — 두 번 쓰입니다.
   진짜 토큰을 받는 시점에 함께 비웁니다.

### 계정 구분

카카오 로그인에는 아이디가 없어서, 토큰(JWT) 안의 `sub` 를 꺼내
`kakao:99` 같은 형태로 이 기기 안의 계정을 구분합니다. `sub` 가 없으면 닉네임을 씁니다.

---

## 카카오 로그인 토큰 발급 (연동 완료)

`POST /api/auth/kakao/complete` — 요청 `{ onboardingToken }` (1.2-1 에서 받은 값 그대로)

응답은 1.2-1 과 같은 구조이고, 이번에는 `token` 이 들어옵니다.

| 상태코드 | 화면 동작 |
|---|---|
| 200 | 토큰 저장 → 반가워요 화면 |
| 200 + `isNewUser:true` | **아직 안 끝난 것** — 아래 참고 |
| 401 | "인증 시간이 지났어요. 카카오 로그인을 다시 해주세요." |
| 404 | "계정을 찾을 수 없어요. 다시 로그인해 주세요." |
| 409 | "이미 탈퇴한 계정이에요." |
| 500 | "서버에 문제가 생겼어요." |

### 200 인데 로그인이 안 된 경우

명세에 `isNewUser` 가 "여전히 온보딩 미완료면 true", `onboardingToken` 이
"여전히 미완료 시 새 토큰 재발급"으로 되어 있습니다.
즉 **성공(200)인데 토큰이 안 오는 경우**가 있습니다.

이때 그냥 다음 화면으로 보내면 **로그인이 안 된 채로 앱에 들어가게 됩니다.**
그래서 `token` 이 없으면 넘어가지 않고 "저장이 끝나지 않았어요. 한 번만 더 눌러 주세요." 를 띄웁니다.
새로 발급된 `onboardingToken` 은 이미 받아 뒀으므로, 버튼을 다시 누르면 그대로 이어집니다.

---

## 토큰 재발급 (연동 완료)

`POST /api/auth/refresh`

요청 헤더

| type | value |
|---|---|
| Authorization | Bearer {accessToken} |
| X-Refresh-Token | {refreshToken} |

응답 `200 OK`

```json
{ "success": true, "data": { "accessToken": "...", "refreshToken": "..." }, "error": null }
```

| 상태코드 | 화면 동작 |
|---|---|
| 200 | 새 토큰으로 교체하고, 실패했던 요청을 자동으로 다시 시도 |
| 400 | "입력한 내용을 다시 확인해 주세요." |
| 401 | 토큰을 지우고 로그아웃 → "로그인이 만료됐어요. 다시 로그인해 주세요." |
| 500 | "서버에 문제가 생겼어요. 잠시 후 다시 시도해 주세요." |

**직접 부를 일은 거의 없습니다.** 어떤 요청이든 `401` 이 오면 `api.js` 가 알아서
재발급을 한 번 시도하고 원래 요청을 다시 보냅니다. 재발급도 실패하면 그때 로그아웃됩니다.
여러 요청이 동시에 401 을 받아도 재발급은 한 번만 나갑니다.

---

## 로그아웃 (연동 완료)

`POST /api/auth/logout`

요청 헤더

| type | value |
|---|---|
| Authorization | Bearer {accessToken} |

응답 `200 OK`

```json
{ "success": true, "data": null, "error": null }
```

| 상태코드 | 화면 동작 |
|---|---|
| 200 | 토큰·세션 정리 후 시작 화면으로 |
| 401 | 토큰이 이미 만료된 것이므로, 재발급 시도 없이 그대로 로그아웃 |
| 500 | 서버 오류여도 화면에서는 로그아웃 처리 |

> 서버 응답이 어떻든 **화면에서는 항상 로그아웃됩니다.** 서버에 알리지 못했다고 해서
> 사용자를 로그인 상태로 붙잡아 두면 곤란하기 때문입니다.
> 로그인하지 않은 상태에서 부르면 서버 호출 없이 바로 끝냅니다.

---

## 내 정보 조회 (연동 완료)

`GET /api/users/me`

```json
{ "nickname": "닉네임", "email": "hong@gmail.com", "pointBalance": 1200,
  "interestCategoryCodes": ["NUTRITION_SUPPLEMENT","COSMETICS"] }
```

> **필드 이름 주의.** 관심분야는 `interestCategories` 가 아니라
> **`interestCategoryCodes`** 입니다. 저장(온보딩)할 때 쓰는 이름과 같습니다.
> 프론트가 `interestCategories` 로 읽고 있어서, 관심분야를 설정해 둔 사람에게도
> 마이페이지에 **'미설정'** 이 뜨고 있었습니다. (2026-08-20 수정)
>
> `ageGroup`·`gender`·`profileImageUrl`·`createdAt` 은 이 응답에 없습니다.
> 나이대·성별은 온보딩 때 저장한 값을 기기에서 그대로 씁니다.

| 상태코드 | 화면 동작 |
|---|---|
| 200 | 닉네임·관심분야·포인트를 서버 값으로 |
| 401 | 아래 참고 — **로그아웃시키지 않습니다** |
| 500 | 저장된 값 그대로 두고 넘어감 |

### 이걸로 바뀐 것 두 가지

**1. 누적 포인트를 더 이상 계산하지 않습니다.**
7.2 내역만 있을 때는 100건씩 끝까지 받아 더했는데, 이제 `pointBalance` 를 그대로 씁니다.
요청 수도 줄고 값도 정확합니다.

**2. 닉네임이 기기마다 달라지지 않습니다.**
전에는 서버가 닉네임을 안 줘서 아이디로 만든 임시 닉네임을 썼습니다.
이제 로그인 직후 서버 값으로 맞춥니다.

### 부르는 시점

- 로그인 직후 (아이디 로그인 / 카카오 로그인 / 카카오 온보딩 완료)
- 마이페이지, 내 정보 수정, 관심분야 변경 — **저장된 값을 먼저 보여주고 서버 값이 오면 조용히 바꿉니다**

화면을 비워두고 기다리지 않습니다. 느린 네트워크에서 빈 화면을 보는 것보다 낫습니다.

### 401 이 와도 로그아웃시키지 않습니다

로그인 직후 부르는 요청이라, 여기서 실패했다고 세션을 끊으면
**방금 로그인한 사람이 곧바로 튕겨 나갑니다.** 그래서 이 호출만 401 자동 처리를 끕니다.
프로필 없이 저장된 값으로 그냥 진행합니다.

### 나이대는 아직 안 맞습니다

응답이 `AGE_50S` 인데 화면 선택지는 `10~24 / 25~39 / 40~59 / 60세 이상` 입니다.
표에 없는 값이 와도 `AGE_50S` 를 그대로 보여주지 않도록 **'50대'로 바꿔 읽습니다.**

> 다만 이건 보여줄 때만 해결된 것이고, **저장할 때는 여전히 화면의 4구간 값을 보냅니다.**
> 서버 기준이 10년 단위라면 화면 선택지를 바꾸는 게 맞습니다. 알려주세요.

---

## 내 정보 수정 (연동 완료)

`PATCH /api/users/me/nickname` — 요청 `{ nickname?, profileImageUrl? }` (바꿀 것만)

| 상태코드 | 화면 동작 |
|---|---|
| 200 | 저장 후 내 정보 수정 화면으로 |
| 400 | **닉네임 칸에 빨간 테두리** + "이미 사용 중이거나 쓸 수 없는 닉네임이에요." |
| 401 | 토큰 재발급 후 자동 재시도 |
| 500 | "서버에 문제가 생겼어요." |

들어가는 곳: 마이페이지 → 내 정보 수정 → 닉네임

### 400 을 닉네임 칸에 표시하는 이유

**중복 닉네임 전용 상태코드(409)가 명세에 없습니다.** 길이·형식은 화면에서 이미 걸러내고
보내므로, 여기서 오는 400 은 대개 "이미 쓰는 닉네임"입니다.
그래서 화면 아래 오류 문구가 아니라 **입력 칸에 바로** 표시합니다.

> 서버가 중복을 409 로 따로 준다면 알려주세요. 문구를 "이미 사용 중인 닉네임이에요."로
> 정확히 나눌 수 있습니다.

### 프로필 이미지는 안 보냅니다

`profileImageUrl` 도 받는 API 지만, 사진을 고르는 기능을 빼기로 해서 지금은 닉네임만 보냅니다.
나중에 필요하면 `API.updateMe({ profileImageUrl })` 로 그대로 쓸 수 있습니다.

---

## 온보딩 정보 저장 (연동 완료)

`POST /api/me/onboarding`

| 파라미터 | 타입 | 설명 |
|---|---|---|
| onboardingToken | String | 카카오 로그인 응답에서 받은 값 (있을 때만 보냄 — 아래 참고) |
| interestCategoryCodes | String[] | 관심 카테고리 **다중 선택, 1개 이상** |
| ageGroup | String | 나이대 |
| gender | String | 성별 |

응답 `201 Created`

```json
{ "success": true,
  "data": { "interestCategories": ["NUTRITION_SUPPLEMENT","COSMETICS"],
            "ageGroup": "AGE_50S", "gender": "FEMALE", "onboardingCompleted": true } }
```

| 상태코드 | 화면 동작 |
|---|---|
| 201 | 저장 후 홈으로 이동 |
| 400 | "입력한 내용을 다시 확인해 주세요." |
| 401 | onboardingToken 을 보낸 경우 → "인증 시간이 지났어요. 다시 로그인한 뒤 시도해 주세요."<br>안 보낸 경우 → 액세스 토큰 재발급 후 자동 재시도 |
| 404 | "계정을 찾을 수 없어요. 다시 로그인해 주세요." |
| 500 | "서버에 문제가 생겼어요." |

회원가입 3단계(성별·연령대)에서 고른 값을 모아뒀다가, 4단계에서 관심분야와 **함께 한 번에** 보냅니다.

### onboardingToken — 지금은 보낼 값이 없습니다

명세에는 필수인데, **카카오 로그인 응답에서 받는 값**이라 지금 우리 앱에는 없습니다.
카카오 연동을 빼기로 하셔서 삭제했고, 지금 가입/로그인은 아이디·비밀번호(1.1 / 1.2)입니다.

그래서 이렇게 넣어 뒀습니다.

- 로그인 응답에 `onboardingToken` 이 **들어 있으면 받아서 보관**했다가 2.1 요청에 담습니다
- 없으면 아예 안 담고, 대신 `Authorization: Bearer {accessToken}` 으로 본인을 확인합니다
- 한 번 쓴 토큰은 지웁니다 (1회용으로 보고)

> **확인 부탁드립니다.** 아이디·비밀번호로 가입한 사람은 이 토큰을 받을 방법이 없습니다.
> 서버가 `onboardingToken` 없이 `Authorization` 헤더만으로도 받아준다면 지금 그대로 동작하고,
> 반드시 필요하다면 **1.1 회원가입이나 1.2 로그인 응답에 같이 내려주셔야** 합니다.
> (내려주기만 하면 프론트는 고칠 게 없습니다 — 자동으로 담습니다)

### 필드 이름이 저장과 수정에서 다릅니다

| | 관심 카테고리 필드 |
|---|---|
| 2.1 저장 (POST) | `interestCategoryCodes` |
| 2.2 수정 (PATCH) | `interestCategories` |
| 응답 (양쪽 다) | `interestCategories` |

명세서에 그렇게 적혀 있어서 그대로 따랐습니다. **같은 이름이 맞다면**
`api.js` 의 `INTEREST_FIELD` 두 줄만 맞추면 됩니다.

> 요청 예시의 값도 다릅니다. 2.1 예시는 `["NUTRITION"]`, 응답 예시는 `["NUTRITION_SUPPLEMENT"]` 입니다.
> 지금은 응답 예시를 따라 `NUTRITION_SUPPLEMENT` 를 보내고 있습니다. 400 이 나면 이 값입니다.

---

## 온보딩 정보 수정 (연동 완료)

`PATCH /api/me/onboarding`

| 파라미터 | 타입 | 설명 |
|---|---|---|
| interestCategories | String[] | 변경할 관심 카테고리 (선택) |
| ageGroup | String | 변경할 나이대 (선택) |
| gender | String | 변경할 성별 (선택) |

응답 `200 OK` — 2.1 과 같은 구조 (수정된 값 반영)

**바꾼 항목만 보냅니다.** 마이페이지 → 내 정보 수정 → 관심분야 변경에서 관심분야만 고치면
`{ "interestCategories": [...] }` 만 나가고 나이대·성별은 건드리지 않습니다.

> 아직 최초 저장(POST)을 안 한 계정이면 PATCH 대신 POST 로 자동 전환됩니다.
> 화면에서는 `API.updateOnboarding()` 하나만 부르면 되고, 어느 쪽으로 나갈지는 알아서 정합니다.

### 화면 문구 ↔ 서버 값

`data.js` 의 아래 표에서 관리합니다. **서버 enum 이 확정되면 value 만 고치면 됩니다.**

| 화면 | 지금 보내는 값 |
|---|---|
| 다이어트 | `DIET` |
| 미용/화장품 | `COSMETICS` |
| 건강/면역 | `NUTRITION_SUPPLEMENT` |
| 기타 | `ETC` |
| 10~24세 / 25~39세 / 40~59세 / 60세 이상 | `AGE_10S` / `AGE_20S` / `AGE_40S` / `AGE_60S` |
| 여성 / 남성 / 기타 | `FEMALE` / `MALE` / `OTHER` |

> **확인이 필요합니다.** 예시에 나온 `NUTRITION_SUPPLEMENT`, `COSMETICS`, `AGE_50S`, `FEMALE` 만 보고
> 나머지는 추측해서 넣었습니다. 400 이 나면 이 표가 서버 enum 과 다른 것이니,
> Swagger 에서 전체 목록을 확인해 `data.js` 만 고치면 됩니다.
>
> 특히 **나이대**가 문제입니다. 화면 목업은 `10~24 / 25~39 / 40~59 / 60+` 인데
> 서버는 `AGE_50S` 처럼 10년 단위로 보입니다. 서버 기준이 맞다면 화면 선택지를
> `10대 / 20대 / 30대 / 40대 / 50대 / 60대 이상` 으로 바꾸는 게 맞습니다.

---

## 오늘의 브리핑 조회 (연동 완료)

`GET /api/briefings/today` — 요청 파라미터 없음

응답 `200 OK`

```json
{ "success": true, "data": { "date": "2026-08-05", "items": [
  { "categoryCode": "NUTRITION_SUPPLEMENT",
    "trustLevel": "CLINICAL_EVIDENCE",
    "target": "비타민C 고함량 제품",
    "effect": "면역력 개선",
    "evidenceSummary": "..." } ] } }
```

> **위 구조가 실제 응답입니다** (`/v3/api-docs` 의 `BriefingItemResponse`).
> 예전에 이 문서에 적혀 있던 `briefingId`·`title`·`summary`·`trustLevelLabel`·
> `relatedArchiveId` 는 **오지 않습니다.** 그걸 읽고 있어서 브리핑 카드가
> 전부 빈 줄로 나왔습니다. 지금은 이렇게 만들어 씁니다. (2026-08-20 수정)
>
> | 화면 | 만드는 방법 |
> |---|---|
> | 제목 | `target` + `effect` 를 ` · ` 로 이음 |
> | 설명 | `evidenceSummary` |
> | 분야 칩 | `categoryCode` → 한글 라벨 |
> | 신뢰도 라벨 | `trustLevel` → 한글 라벨 |
>
> **`briefingId` 가 없어서 열람 기록(4.3)을 보낼 수 없습니다.** 카드를 눌러도
> `POST /briefings/{briefingId}/open` 을 부르지 못합니다. 오픈율 지표가
> 필요하면 응답에 `briefingId` 를 넣어 주세요.

들어가는 곳: 홈 상단 배너, 오늘의 브리핑 화면

- `category` 는 온보딩과 같은 enum 이라 한글 라벨로 바꿔 표시합니다
- 카드를 누르면 `relatedArchiveId` 로 상세 화면에 갑니다
- 홈 배너는 받아온 카드로 바뀌고, **실패하면 기본 배너를 그대로 둡니다** (화면이 비지 않도록)

> **날짜 선택을 없앴습니다.** 원래 최근 7일을 고를 수 있게 해뒀는데, 이 API 는 오늘 것만
> 주기 때문에 응답의 `date` 를 표시만 합니다. 지난 브리핑도 보게 하려면
> `GET /briefings?date=` 같은 API 가 필요해요.

> 카드가 1~2건이라 카테고리 칩은 **받아온 카드에 있는 분야만** 보여줍니다.

---

## 판정 요청 생성 (연동 완료)

`POST /api/judgements`

요청 헤더 — `Authorization: Bearer {accessToken}`, `Content-Type: application/json`

| 파라미터 | 타입 | 설명 |
|---|---|---|
| inputType | String | `TEXT` / `IMAGE` / `LINK` |
| text | String | 원문 텍스트 (TEXT 일 때 필수) |
| imageBase64 | String | 캡처 이미지 base64 (IMAGE 일 때 필수) |
| url | String | 유튜브·SNS 링크 (LINK 일 때 필수) |
| categoryId | Long | 선택 (없으면 AI 자동 분류) |

응답 `202 Accepted`

```json
{ "success": true, "data": { "judgmentId": 1, "status": "PROCESSING" } }
```

| 상태코드 | 화면 동작 |
|---|---|
| 202 | 접수 → 판정 결과 화면으로 이동 |
| 400 | "입력한 내용을 다시 확인해 주세요." |
| 401 | 토큰 재발급 후 자동 재시도 |
| 422 | **추출 실패 안내 화면**(`judge-fail.html`)으로 이동 |
| 429 | **한도 초과 안내 화면**(`judge-limit.html`)으로 이동 |
| 500 | "서버에 문제가 생겼어요." |

이미지는 파일을 읽어 `data:image/png;base64,` 접두어를 떼고 순수 base64 문자열만 보냅니다.

202 는 접수만 알려주므로, 곧바로 아래 조회 API 로 결과를 받아옵니다.

---

## 판정 결과 조회 (연동 완료)

`GET /api/judgements/{judgementId}`

응답에서 화면 값으로 이렇게 바꿔 씁니다.

| 서버 | 화면 |
|---|---|
| `trustLevel: CLINICAL_EVIDENCE` | 임상적 근거 있음 |
| `trustLevel: EXPERT_OPINION` | 전문가 의견 있음 |
| `trustLevel: PENDING` | 판단보류 |
| `trustLevel: NO_EVIDENCE` | 근거 부족 |
| `trustLevel: COUNTER_EVIDENCE` | 반박 근거 있음 |
| `extractedText` | 판정한 주장 문장 |
| `trustLevelLabel` | 화면에 그대로 표시 (내부 등급보다 우선) |
| `conflictOfInterest.badgeLabel` / `.description` | 이해상충 카드 |
| `sources[]` | 근거 요약 (url 있으면 링크로) |
| `guideCard.title` / `.tips[]` | 구매 기준 카드 |
| `safetyNotice` | 의료 안전 안내 문구 |

| 상태코드 | 화면 동작 |
|---|---|
| 200 | 결과 표시 |
| 401 | 토큰 재발급 후 자동 재시도 |
| 403 | "내가 요청한 판정만 볼 수 있어요." |
| 404 | "판정 결과를 찾을 수 없어요." |
| 500 | "서버에 문제가 생겼어요." |

### PROCESSING 처리

`status` 가 `PROCESSING` 이면 결과 화면에 로딩 표시를 띄우고 **1.5초 간격으로 다시 물어봅니다.**
`DONE` 이 되면 화면을 채우고, `FAILED` 면 추출 실패 안내 화면으로 넘어갑니다.
60초가 지나도 안 끝나면 "판정이 오래 걸리고 있어요. 잠시 후 판정 이력에서 확인해 주세요."가 뜹니다.

간격이나 제한 시간은 `api.js` 의 `waitForJudgment` 기본값에서 바꿀 수 있습니다.

`DONE` 이 되는 순간 마이페이지 판정 이력에도 자동으로 쌓입니다 (같은 판정은 한 번만).

---

## 판정 히스토리 조회 (연동 완료)

`GET /api/judgements?page=&size=&categoryId=`

| 파라미터 | 기본값 | 설명 |
|---|---|---|
| page | 1 | 페이지 번호 |
| size | 20 | 페이지 크기 |
| categoryId | — | 카테고리 필터 (선택) |

응답 `200 OK`

```json
{ "success": true,
  "data": { "items": [ { "judgmentId": 1, "trustLevelLabel": "근거 부족", "categoryId": 3, "createdAt": "..." } ],
            "hasNext": true } }
```

들어가는 곳: 마이페이지 → 판정 이력 탭

- 탭을 처음 열 때 1페이지를 부르고, `hasNext` 가 true 면 **더 보기** 버튼이 나옵니다
- `trustLevelLabel` 로 적합·애매·부적합 뱃지를 정합니다
  (임상적 근거 있음 / 전문가 의견 있음 → 적합, 판단보류 / 근거 부족 → 애매, 반박 근거 있음 → 부적합)
- 위쪽 통계는 **지금까지 불러온 만큼** 세고, 더 남아 있으면 `3+` 처럼 표시합니다

> **items 에 판정 문장(제목)이 없습니다.** 지금은 이 기기에 저장된 문장이 있으면 그걸 쓰고,
> 없으면 `판정 #1` 로 보여줍니다. 다른 기기에서 본 이력은 제목이 비어 보여요.
> `items` 에 `extractedText` 나 요약 제목을 한 줄 넣어주시면 목록이 훨씬 읽기 좋아집니다.

> `categoryId` 필터를 쓰려면 카테고리 목록 API(id ↔ 이름)가 필요합니다. 지금은 전체만 조회합니다.

---

## 특정 날짜 브리핑 조회 (연동 완료)

`GET /api/briefings/{date}` — `date` 는 `YYYY-MM-DD` (경로 변수)

응답은 4.1 과 같은 구조이고 `date` 만 요청한 날짜로 바뀝니다.

| 상태코드 | 화면 동작 |
|---|---|
| 200 | 그 날 카드 목록 |
| 400 | "날짜 형식이 올바르지 않아요." |
| 401 | 토큰 재발급 후 자동 재시도 |
| 404 | **오류가 아니라 "이 날은 브리핑이 없어요."** 로 비어 있게 표시 |
| 500 | "서버에 문제가 생겼어요." |

### 화면

브리핑 화면 날짜 옆에 **‹ ›** 를 넣었습니다. 왼쪽은 하루 전, 오른쪽은 하루 뒤입니다.

- **오늘이면 `‹` 만 눌립니다.** 앞으로 갈 날은 아직 없으니 `›` 는 잠급니다.
- 오늘 날짜는 `getBriefing()` 이 **4.1(`/briefings/today`)로 돌려보냅니다.** 같은 내용을 두 경로로 부를 필요가 없어서입니다.
- 404 는 오류 화면 대신 빈 상태로 보여 주고, **거기서도 계속 이동할 수 있습니다.** 브리핑이 없는 날이 중간에 있어도 막히지 않습니다.
- 날짜 형식은 보내기 전에 프론트에서 한 번 봅니다. 400 을 만들 일이 없습니다.

`briefing.html?date=2026-08-01` 로 열면 그 날짜부터 봅니다. '브리핑 공유' 버튼도 보고 있는
날짜가 담긴 링크를 복사합니다. (미래 날짜를 넣으면 무시하고 오늘을 보여 줍니다)

---

## 브리핑 열람 기록 (연동 완료)

`POST /api/briefings/{briefingId}/open`

응답 `200 OK` — `{ "success": true, "data": { "opened": true } }`

오픈율 지표용이라 **화면을 막지 않습니다.** 어떤 상태코드가 와도 사용자에게는 아무것도 보이지 않습니다.

| 상태코드 | 화면 동작 |
|---|---|
| 200 · 401 · 404 · 500 | **전부 동일 — 아무 표시 없이 그대로 진행** (실패는 콘솔에만) |

보내는 시점

- 브리핑 화면에서 **카드를 누를 때** (상세로 넘어가기 직전)
- 홈 캐러셀에서 **브리핑 배너를 누를 때** — 단, 서버에서 받은 카드일 때만.
  기본 배너는 `data.js` 의 임시 id 라서 서버에 없는 값입니다.

### 이동하면서 보내도 안 끊기게

카드를 누르면 곧바로 다음 화면으로 넘어가는데, 그냥 보내면 이동하면서 요청이 잘립니다.
그래서 이 요청만 **`keepalive: true`** 로 보냅니다. 페이지가 바뀌어도 요청은 끝까지 갑니다.
지표 때문에 사용자를 기다리게 할 수는 없으니 응답은 기다리지 않습니다.

### 401 이어도 로그인 안내를 띄우지 않습니다

다른 API 는 401 이면 토큰을 재발급하고 "로그인이 만료됐어요" 를 띄우는데,
이건 지표 수집이라 그렇게 하면 안 됩니다. 카드 한 번 눌렀다고 로그인 화면으로 튕기면 이상하니까요.
**재발급도 안 하고, 안내도 안 띄우고, 조용히 지나갑니다.**

같은 브리핑은 한 번만 보냅니다(더블클릭 방지). 실패한 건은 다시 시도할 수 있게 열어 둡니다.

---

## 판정 결과 공유 링크 생성 (연동 완료)

`POST /api/judgments/{judgmentId}/share`

응답 `201 Created`

```json
{ "success": true, "data": {
  "shareToken": "9f2a7b3c1d4e5f60a1b2c3d4e5f60718",
  "shareUrl": "https://doctorjudge.app/share/9f2a...",
  "imageUrl": "https://cdn.doctorjudge.app/share/9f2a....png" } }
```

| 상태코드 | 화면 동작 |
|---|---|
| 201 | 발급받은 `shareUrl` 로 공유 시트 열기 (안 되면 링크 복사) |
| 401 | 토큰 재발급 후 자동 재시도 |
| 403 | "내가 요청한 판정만 볼 수 있어요." |
| 404 | "판정 결과를 찾을 수 없어요." |

들어가는 곳: 판정 결과 → 링크로 공유하기 → 확인 창 → 공유하기

전에는 현재 브라우저 주소를 그대로 공유했는데, 이제 **서버가 발급한 공유 전용 주소**를 씁니다.
로그인 없이 열리는 링크라 확인 창 문구("로그인 없이 누구나 열어볼 수 있습니다")와도 맞습니다.

> ⚠️ **경로 철자에 주의하세요.** 다른 판정 API 는 `/api/judgements` (e 있음) 인데
> 이 API 만 명세서에 `/api/judgments` (e 없음) 로 적혀 있습니다.
> 오타가 아니라면 그대로 두면 되고, 404 가 나면 `api.js` 의 `SHARE_PATH` 한 줄만 고치면 됩니다.

> `imageUrl` (공유용 요약 카드 이미지)은 아직 화면에 쓰지 않고 받아만 둡니다.
> 카카오톡 공유처럼 이미지가 필요한 곳이 생기면 바로 쓸 수 있어요.

---

## 공유 피드에 게시 (연동 완료)

`POST /api/feed/posts`

| 파라미터 | 타입 | 설명 |
|---|---|---|
| judgmentId | String | 게시할 판정 ID (Request Body) |

응답 `201 Created`

```json
{ "success": true, "data": { "postId": "p_5001", "status": "PUBLISHED" } }
```

| 상태코드 | 화면 동작 |
|---|---|
| 201 | 내 공유 카드에 저장하고 마이페이지로 이동 |
| 400 | "아직 게시할 수 없는 판정이에요. 판정이 끝난 뒤 다시 시도해 주세요." |
| 401 | 토큰 재발급 후 자동 재시도 |
| 404 | "판정 결과를 찾을 수 없어요." |
| 500 | "서버에 문제가 생겼어요." |

들어가는 곳: 판정 결과 → 공유 피드에 게시하기

- 성공해야만 내 공유 카드에 저장합니다. 실패하면 화면에 남아 다시 시도할 수 있어요
- 받은 `postId` 와 `judgmentId` 를 카드에 함께 저장해서, 나중에 공유 회수에 씁니다

---

## 공유 피드 전체 목록 (연동 완료)

`GET /api/feed/posts?sort=&page=&size=`

> **2026-08-20** 이 API 가 예전에 500 을 내서, 프론트는 한동안
> `GET /feed/posts/me`(내 게시물)로 대신 쓰고 있었습니다. 그래서 공유 피드에
> 내 글만 보였습니다. 지금은 전체 피드를 먼저 부르고, 실패하면 예전처럼
> 내 게시물로 물러나도록 해 두었습니다.

| 파라미터 | 기본값 | 설명 |
|---|---|---|
| sort | recent | `recent` / `popular` |
| page | 1 | 페이지 번호 |
| size | 20 | 페이지 크기 |

응답 `200 OK`

```json
{ "success": true, "data": {
  "items": [ { "postId": "p_5001", "author": { "userId": "u_1001", "nickname": "닉네임" },
               "trustLevelLabel": "근거 부족", "summary": "...", "likeCount": 12,
               "createdAt": "2026-08-05T10:10:00Z" } ],
  "page": 1, "totalPages": 8 } }
```

들어가는 곳: 피드 탭

- 최신순 / 인기순 필이 그대로 `sort` 로 나갑니다
- `page < totalPages` 면 아래에 **더 보기** 버튼이 나옵니다
- 정렬을 바꾸면 1페이지부터 다시 받습니다

> **items 에 제목과 카테고리가 없습니다.** `summary` 를 카드 제목처럼 크게 쓰고,
> 카테고리 자리에는 작성 시각을 넣었습니다. 목업의 "건강 · 질환/증상관리" 같은 분류를
> 살리려면 `category` 를, 따옴표 제목을 살리려면 `title` 을 추가해 주세요.

> 좋아요 수(`likeCount`)는 보여주기만 합니다. 누르는 기능은 좋아요 API 가 필요해요.

---

## 내가 올린 피드 게시물 (연동 완료)

`GET /api/feed/posts/me?page=&size=` — 6.5 와 같은 응답 구조, 본인 것만

들어가는 곳: 마이페이지 → 나의 공유 카드 탭

- 탭을 처음 열 때 1페이지를 부르고, `page < totalPages` 면 **더 보기** 가 나옵니다
- 카드 옆 휴지통은 공유 회수(6.3)로 이어집니다

> 휴지통은 게시물 삭제(6.8)를 부르므로 `postId` 만 있으면 됩니다. 추가 필드는 필요 없습니다.
> 다만 목록에 제목이 없어서, 이 기기에서 올린 글이 아니면 `summary` 를 제목으로 씁니다.

---

## 피드 게시물 좋아요 / 취소 (연동 완료)

| 동작 | 메서드 | URL |
|---|---|---|
| 좋아요 | `POST` | `/api/feed/posts/{postId}/like` |
| 좋아요 취소 | `DELETE` | `/api/feed/posts/{postId}/like` |

URL 은 같고 메서드로 구분합니다. 하트를 누르면 지금 상태를 보고
켜는 중이면 POST, 끄는 중이면 DELETE 를 보냅니다.

응답 `200 OK`

```json
{ "success": true, "data": { "liked": true, "likeCount": 13 } }
```

| 상태코드 | 화면 동작 |
|---|---|
| 200 | 서버가 준 `liked`·`likeCount` 로 하트와 숫자를 맞춤 |
| 401 | 토큰 재발급 후 자동 재시도 |
| 404 | "판정 결과를 찾을 수 없어요." |
| 500 | 오류 안내, 하트와 숫자는 그대로 |

들어가는 곳: 피드 탭의 카드 하트

- 화면에서 미리 바꾸지 않고 **서버 응답이 온 뒤에** 하트와 숫자를 바꿉니다.
  실패했는데 숫자가 늘어나 보이는 일이 없도록요
- 하트를 눌러도 카드 상세로 이동하지 않습니다
- 응답 오는 동안 버튼을 잠가서 연타로 여러 번 나가지 않게 했습니다

> **6.5 목록에 `liked` 가 없습니다.** 그래서 목록을 처음 받으면 내가 이미 누른 글도
> 빈 하트로 보입니다. `items` 에 `liked` 를 넣어주시면 바로 반영되게 해뒀어요.

---

## 피드 게시물 삭제 (연동 완료)

`DELETE /api/feed/posts/{postId}`

응답 `200 OK` — `{ "success": true, "data": null }`

| 상태코드 | 화면 동작 |
|---|---|
| 200 | 목록에서 카드 제거 |
| 403 | "내가 요청한 판정만 볼 수 있어요." |
| 404 | 이미 없는 글로 보고 **목록에서 제거** |
| 500 | 오류 문구 표시, 카드는 그대로 |

들어가는 곳: 마이페이지 → 나의 공유 카드 → 휴지통 → "게시물을 내릴까요?"

> 이 API 덕분에 **6.6 목록에 `judgmentId` 를 넣어달라는 요청은 없어도 됩니다.**
> 원래는 휴지통이 공유 링크 회수(6.3)를 부르느라 `judgmentId` 가 필요했는데,
> 지금은 `postId` 로 게시물을 삭제하므로 목록에 있는 값만으로 충분합니다.

> 공유 링크 회수(6.3)와는 다른 동작입니다.
> 삭제는 피드에서 글을 내리는 것이고, 회수는 링크를 못 쓰게 막는 것입니다.

---

## 공유 링크 회수 (연동 완료)

`DELETE /api/judgments/{judgmentId}/share`

응답 `200 OK` — `{ "success": true, "data": null }`

| 상태코드 | 화면 동작 |
|---|---|
| 200 | 목록에서 카드 제거 |
| 401 | 토큰 재발급 후 자동 재시도 |
| 403 | "내가 요청한 판정만 볼 수 있어요." |
| 404 | 이미 회수된 것으로 보고 **목록에서 제거** |
| 500 | 오류 문구 표시, 카드는 그대로 둠 |

들어가는 곳: 마이페이지 → 나의 공유 카드 → 휴지통 → "공유 중지" 확인 창

- 회수하면 그 링크로 들어온 사람은 6.2 에서 `410` 을 받고 "만료된 링크예요" 를 봅니다
- 404(이미 회수됨)는 오류로 취급하지 않고 목록에서 지웁니다 — 사용자가 원한 결과는 이미 이뤄진 상태라서요
- 500 일 때는 카드를 남겨둡니다. 서버엔 살아 있는데 화면에서만 사라지면 안 되니까요

> 공유 피드에 게시할 때 `judgmentId` 를 카드에 함께 저장해 두고, 회수할 때 그 값을 씁니다.

---

## 공유 링크 공개 조회 (연동 완료)

`GET /api/share/{shareToken}` — **인증 헤더 없음** (비회원도 열람)

응답 `200 OK`

```json
{ "success": true, "data": {
  "title": "크랜베리가 방광염 예방에 도움이 된다",
  "trustLevel": "NO_EVIDENCE",
  "evidenceSummary": "...",
  "conflictDetected": true,
  "conflictDescription": "...",
  "safetyNotice": "...",
  "guideCardJson": "{\"title\":\"영양제 확인 사항\",\"tips\":[\"...\"]}" } }
```

> **3.2(판정 결과 조회)와 구조가 다릅니다.** '작성자 정보만 뺀 같은 구조'가
> 아닙니다. 이걸 3.2 처럼 읽고 있어서 공유 링크를 열면 판정 내용이 통째로
> 비어 있었습니다. (2026-08-20 수정)
>
> | 다른 점 | 3.2 판정 결과 | 9.3 공유 조회 |
> |---|---|---|
> | 판정 문장 | `extractedText` | **`title`** |
> | 신뢰도 라벨 | `trustLevelLabel` | **`trustLevel` 만 옴** (한글 라벨은 화면에서 만듦) |
> | 이해상충 | `conflictOfInterest.detected` | **`conflictDetected`** (평평함) |
> | 구매 기준 카드 | `guideCard` (객체) | **`guideCardJson`** (JSON **문자열** — `JSON.parse` 필요) |
> | 근거 출처 | `sources` 배열 | **없음** |

| 상태코드 | 화면 동작 |
|---|---|
| 200 | 판정 결과 표시 |
| 404 | "링크를 찾을 수 없어요" |
| 410 | "만료된 링크예요" |
| 500 | "서버에 문제가 생겼어요." |

이 API 를 위해 **공유 전용 화면 `share.html` 을 새로 만들었습니다.**

- 로그인 상태와 무관하게 열리고, 하단 탭바 없이 결과만 보여줍니다
- 맨 아래에 "Dr.Judge 시작하기" 버튼을 둬서 비회원이 앱으로 넘어올 수 있게 했습니다
- 주소는 `/share/{토큰}` 과 `share.html?token=...` 둘 다 받습니다
- 토큰이 남아 있어도 이 요청에는 `Authorization` 을 붙이지 않고, 401 재발급도 시도하지 않습니다

### Netlify 라우팅

서버가 주는 링크가 `https://.../share/{토큰}` 형태라, 정적 호스팅에서 그 주소가
`share.html` 로 가도록 `_redirects` 에 규칙을 넣었습니다.

```
/share/*  /share.html  200
```

> 배포 도메인이 `doctorjudge.app` 가 아니라면, 서버가 만드는 `shareUrl` 의 도메인도
> 실제 배포 주소로 맞춰야 링크가 열립니다.

---

## 회원 탈퇴 (연동 완료)

`DELETE /api/auth/me`

요청 헤더

| type | value |
|---|---|
| Authorization | Bearer {accessToken} |

응답 `200 OK`

```json
{ "success": true, "data": { "withdrawnAt": "2026-08-16T02:06:35.266" }, "error": null }
```

| 상태코드 | 화면 동작 |
|---|---|
| 200 | 이 기기의 계정 데이터도 지우고 시작 화면으로 |
| 401 | "로그인이 만료됐어요. 다시 로그인해 주세요." (재발급 시도 안 함) |
| 500 | "서버에 문제가 생겼어요." — **계정 데이터는 지우지 않습니다** |

들어가는 곳: 마이페이지 → 톱니 → 회원탈퇴 → 확인 창

> 서버에서 성공 응답이 온 뒤에만 로컬 데이터를 지웁니다. 실패하면 아무것도 건드리지 않아요.
> 탈퇴한 계정으로 다시 로그인하면 서버가 `409` 를 주고, 화면에는 "이미 탈퇴한 계정이에요."가 뜹니다.

---

### 토큰 보관 위치

화면마다 HTML 파일이 나뉘어 있어서, 토큰을 메모리에만 두면 화면을 옮길 때 로그인이 풀립니다.
그래서 계정 데이터와 같은 저장소에 함께 보관합니다.

> 브라우저 저장소는 스크립트가 읽을 수 있습니다. 실제 서비스로 갈 때는
> `httpOnly` 쿠키로 옮기는 편이 안전합니다. 그 경우 `store.js` 의 `saveTokens` 만 비우면 됩니다.

---

## 포인트 내역 조회 (연동 완료)

`GET /api/users/me/points/history?page=1&size=20`

응답 `200 OK`

```json
{ "success": true,
  "data": {
    "items": [
      { "type": "EARN", "reason": "FEED_POST",   "amount": 50, "createdAt": "..." },
      { "type": "EARN", "reason": "DAILY_LOGIN", "amount": 10, "createdAt": "..." }
    ],
    "page": 1, "totalPages": 3 } }
```

| 상태코드 | 화면 동작 |
|---|---|
| 200 | 누적 포인트 + 내역 목록 |
| 401 | 토큰 재발급 후 자동 재시도 |
| 500 | "잠시 후 다시 시도해 주세요." (화면은 그대로) |

들어가는 곳: 마이페이지 → 포인트 탭

### type / reason 처리

- `type` 은 `EARN`(적립) / `USE`(사용). `amount` 는 양수로 오고, 부호는 `type` 으로 판단합니다.
  적립은 `+50P`(초록), 사용은 `−30P`(회색)로 보여 줍니다.
- `reason` 코드는 `data.js` 의 `POINT_REASON` 에서 한글 문구로 바꿉니다.
  **확인된 값은 `DAILY_LOGIN`, `FEED_POST` 두 개**뿐이라 나머지는 예상값을 넣어 뒀습니다.

| reason | 화면 문구 | 확인 |
|---|---|---|
| `DAILY_LOGIN` | 매일 접속 | 확인됨 |
| `FEED_POST` | 공유 카드 게시 | 확인됨 |
| `SIGNUP` / `ONBOARDING` | 가입 축하 / 관심 분야 설정 | 예상 |
| `JUDGMENT` / `SHARE` | 판정 완료 / 판정 결과 공유 | 예상 |
| `LIKE_RECEIVED` / `EVENT` / `ADMIN` | 좋아요 받기 / 이벤트 보상 / 관리자 조정 | 예상 |
| `REWARD_EXCHANGE` / `GIFTICON` | 리워드 교환 / 기프티콘 교환 | 예상 |

**모르는 코드가 와도 화면은 안 깨집니다.** 표에 없는 대문자 코드는 감추고
`포인트 적립` / `포인트 사용` 으로 대신 보여 줍니다. 실제 코드 목록을 주시면 표만 채우면 됩니다.

### 누적 포인트를 구하는 방법

잔액만 주는 API 가 없어서, `getPointSummary()` 가 내역을 끝까지 훑어 더합니다.
한 번에 100건씩 받으므로 대부분 **요청 1번**이면 끝나고, 최대 20페이지(2,000건)까지 봅니다.
목록의 '더 보기'는 이미 받아 둔 배열에서 10건씩 꺼내므로 추가 요청이 없습니다.

> **잔액 API(7.1?) 가 있으면 알려주세요.** `GET /users/me/points` 같은 게 있으면
> 훑는 과정 없이 한 번에 끝납니다. `getPointSummary` 안쪽만 바꾸면 됩니다.

### 로컬 적립은 껐습니다

예전에는 판정 완료·카드 공유 때 브라우저에도 포인트를 쌓았습니다.
이제 서버가 관리하므로 `store.js` 의 `addPoint` 는 `API.LIVE.points` 가 켜져 있으면 아무것도 하지 않습니다.
(양쪽에 쌓이면 숫자가 두 배로 보입니다.)

---

## 아직 명세가 필요한 것들

아래는 프론트에서 이미 호출하고 있어서, 서버 스펙만 알려주시면 바로 맞추겠습니다.
지금은 `USE_MOCK = true` 일 때 브라우저 안에서 흉내만 내고 있습니다.

### 2. 중복 확인 — `GET /auth/check?field=&value=`

- `field` = `loginId` | `email` | `nickname`
- 회원가입 1단계에서 이메일·닉네임을, 2단계에서 아이디를 확인합니다
- 없으면 가입 요청의 409 만으로도 동작은 하지만, 미리 알려주면 사용자 경험이 좋습니다

### 4. 내 데이터 — `GET /users/me`

```json
{ "profile": {...}, "points": [...], "history": [...], "cards": [...] }
```

로그인 직후 이걸 받아서 `Store.hydrate(data)` 한 줄로 채우면, 마이페이지·판정 이력·포인트가 전부 서버 값으로 바뀝니다.

---

## 지금 연동 상태

| 기능 | 상태 |
|---|---|
| 회원가입 | **실서버 연동 완료 (켜짐)** |
| 로그인 | **실서버 연동 완료 (켜짐)** |
| 카카오 로그인 | **실서버 연동 완료 (켜짐)** — REST API 키 필요 |
| 카카오 토큰 발급 | **실서버 연동 완료 (켜짐)** |
| 토큰 재발급 | **실서버 연동 완료 (자동)** |
| 로그아웃 | **실서버 연동 완료 (켜짐)** |
| 회원 탈퇴 | **실서버 연동 완료 (켜짐)** |
| 중복 확인 | 명세 대기 |
| 내 정보 조회 | **실서버 연동 완료 (켜짐)** |
| 내 정보 수정 (닉네임) | **실서버 연동 완료 (켜짐)** |
| 온보딩 최초 저장 (POST) | **실서버 연동 완료 (켜짐)** |
| 온보딩 수정 (PATCH) | **실서버 연동 완료 (켜짐)** |
| 판정 요청 생성 | **실서버 연동 완료 (켜짐)** |
| 판정 결과 조회 | **실서버 연동 완료 (켜짐)** |
| 판정 히스토리 | **실서버 연동 완료 (켜짐)** |
| 오늘의 브리핑 | **실서버 연동 완료 (켜짐)** |
| 특정 날짜 브리핑 | **실서버 연동 완료 (켜짐)** |
| 브리핑 열람 기록 | **실서버 연동 완료 (켜짐)** |
| 공유 링크 생성 | **실서버 연동 완료 (켜짐)** |
| 공유 링크 공개 조회 | **실서버 연동 완료 (켜짐)** |
| 공유 링크 회수 | **실서버 연동 완료 (켜짐)** |
| 공유 피드에 게시 | **실서버 연동 완료 (켜짐)** |
| 공유 피드 목록 | **실서버 연동 완료 (켜짐)** |
| 내가 올린 게시물 | **실서버 연동 완료 (켜짐)** |
| 게시물 삭제 | **실서버 연동 완료 (켜짐)** |
| 좋아요 / 취소 | **실서버 연동 완료 (켜짐)** |
| 포인트 내역 | **실서버 연동 완료 (켜짐)** |

---

## 연동하면서 함께 넣은 것들

명세서에는 없지만 실제로 붙이면 필요한 것들입니다.

### 로그인이 풀렸을 때

토큰 재발급까지 실패하면 `api.js` 가 `drjudge:session-expired` 이벤트를 한 번 쏘고,
`common.js` 가 받아서 안내 후 로그인 화면으로 보냅니다. 원래 보던 화면은 `?next=` 로
기억해뒀다가 로그인 성공하면 그리로 돌려보냅니다.

### 로그인이 필요한 화면 가드

판정 요청(텍스트·이미지·링크), 내 정보 수정, 닉네임/관심분야 변경은 토큰이 없으면
바로 로그인으로 보냅니다. 마이페이지만 예외로, 화면 안에서 "로그인이 필요해요" 안내를 띄웁니다.

### 네트워크 끊김

`navigator.onLine` 을 보고 화면 위에 "인터넷 연결이 끊겼어요" 띠를 띄웁니다.

### 중복 게시 방지

이미 피드에 올린 판정이면 게시 버튼이 "이미 게시했어요" 로 잠깁니다.

### 홈 화면 공유 피드

목업 데이터를 쓰고 있던 홈 하단 피드를 `GET /feed/posts?size=5` 로 바꿨습니다.
검색창은 받아온 목록 안에서 찾고, 일치하는 항목을 위로 올립니다.

### 프로필 사진

사진 고르기 기능은 뺐습니다. 모든 화면에서 기본 캐릭터 이미지를 씁니다.
서버에 프로필 이미지 업로드 API 가 생기면 그때 다시 넣으면 됩니다.

### 피드 상세

게시물 상세 API 가 아직 없어서, 목록에서 카드를 누를 때 그 내용을 넘겨서 상세 화면을 그립니다.
`GET /feed/posts/{postId}` 가 생기면 그걸로 바꾸는 게 좋습니다.

---

## 브라우저에 저장하는 것 (정리 후)

서버가 붙으면서 로컬 저장소를 줄였습니다. 지금 남아 있는 건 이것뿐입니다.

| 저장 항목 | 왜 필요한가 |
|---|---|
| `tokens` | 화면마다 HTML 이 달라서, 메모리에 두면 이동할 때 로그인이 풀립니다 |
| `session` | 지금 누가 로그인했는지 |
| `profile` | 닉네임·이메일·관심분야. **1.6 이 오면 덮어씁니다** (첫 화면을 비우지 않으려고 남겨 둠) |
| `done` | 같은 판정을 두 번 처리하지 않으려고 판정 번호만 |
| `cards` | 6.5·6.6 목록에 `title`·`category` 가 없어서, 내가 올린 글의 원문 보관용 |
| `results` | 판정 결과 캐시 + 피드 상세로 내용 넘기기 |

### 지운 것

- **비밀번호** — v1 때는 로컬 인증 때문에 평문으로 저장했습니다. 서버 로그인으로 바뀌어 필요 없어졌고, 지금은 저장하지 않습니다.
- **로컬 인증·중복확인** — `authenticate`, `isTaken` 제거. 로그인은 서버가, 중복은 가입 409 가 처리합니다.
- **판정 이력 목록** — 3.3 에서 받아옵니다.
- **포인트 적립 내역** — 7.2 에서 받아옵니다. 로컬 적립(`addPoint`)도 함께 껐습니다.

### 예전 데이터 자동 정리

저장 형식에 버전(`v: 2`)을 붙였습니다. 이미 앱을 써본 브라우저에는 **비밀번호가 들어 있는
예전 데이터가 남아 있는데**, 다음에 앱을 열면 자동으로 통째로 비워집니다.
(그때 한 번은 다시 로그인해야 합니다)

콘솔에서 직접 지우려면:

```js
Store.reset()          // 또는
localStorage.removeItem('drjudge')
```

### 서버 API 가 생기면 더 줄일 수 있는 것

| API | 그러면 지울 수 있는 것 |
|---|---|
| (1.6 으로 해결됨) | 포인트 합산·닉네임 임시 생성 |
| 6.5 items 에 `title`·`category` | `cards` |
| `GET /feed/posts/{postId}` | `results` 중 피드 상세용 캐시 |

남는 건 토큰뿐인데, 그것도 `httpOnly` 쿠키로 옮기면 로컬 저장소를 아예 안 써도 됩니다.
