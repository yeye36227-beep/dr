# 백엔드 로컬 실행

## 실행

```bash
docker compose -f docker-compose.local.yml pull   # main 머지될 때마다 최신으로
docker compose -f docker-compose.local.yml up -d
```

- API 서버 → http://localhost:8080
- Swagger → http://localhost:8080/swagger-ui.html
- 끄기 → `docker compose -f docker-compose.local.yml down`
- 로그 → `docker compose -f docker-compose.local.yml logs -f app`

프론트는 따로 Live Server(5500)로 열면 됩니다. `api.js` 는 지금 열려 있는 주소의 8080 을
자동으로 바라보므로 설정을 바꿀 필요가 없습니다.

---

## 앱이 안 뜰 때 — 필수 환경변수 없음 (2026.08.19 재확인)

로그에 `Could not resolve placeholder` 가 보이면 전부 같은 원인입니다.
지금까지 나온 것:

```
Could not resolve placeholder 'KAKAO_CLIENT_ID'            (08.17)
Could not resolve placeholder 'KAKAO_ALLOWED_REDIRECT_URIS' (08.19 — 새로 생김)
Could not resolve placeholder 'FRONTEND_BASE_URL'           (08.19 — 위를 고치면 다음에 걸림)
```

백엔드에 기능이 들어갈 때마다 **필수 환경변수가 늘어나는데**,
`docker-compose.local.yml` 에는 그 값이 안 들어 있어서 부팅 중에 죽습니다.
`pull` 로 이미지를 최신화해도 **compose 파일은 같이 안 따라오기 때문에** 계속 걸립니다.

> **`pull` 만으로는 안 됩니다.** 이미지는 새것인데 compose 가 옛것이라서 생기는 문제예요.

이미지가 요구하는 값은 이렇게 직접 확인할 수 있습니다:

```bash
CID=$(docker create hyunj11/aaction_be:latest) && docker cp "$CID:/app/app.jar" /tmp/drjudge-app.jar && docker rm "$CID"
unzip -p /tmp/drjudge-app.jar BOOT-INF/classes/application.yaml | grep -oE '\$\{[A-Z0-9_]+\}' | sort -u
```

2026.08.19 기준 결과 — 이 11개가 전부 필수입니다:

```
AI_SERVICE_API_KEY  AI_SERVICE_BASE_URL  DB_HOST  DB_NAME  DB_PASSWORD  DB_USERNAME
FRONTEND_BASE_URL  JWT_SECRET  KAKAO_ALLOWED_REDIRECT_URIS  KAKAO_CLIENT_ID  REDIS_HOST
```

`${X:기본값}` 이 아니라 `${X}` 로만 적힌 것이 전부 필수값입니다.

증상이 헷갈립니다. **포트는 열려 있는데 아무 응답이 없어요.**

```
curl: (52) Empty reply from server
```

브라우저에서는 CORS 오류처럼 보이지만 CORS 문제가 아닙니다. 앱이 아예 안 떠 있는 것입니다.

### 해결 — 보충 파일과 함께 실행

원본 파일은 건드리지 않고 `docker-compose.kakao.yml` 을 얹어서 값만 채웁니다.

```bash
docker compose -f docker-compose.local.yml -f docker-compose.kakao.yml up -d
docker compose -f docker-compose.local.yml -f docker-compose.kakao.yml logs -f app
```

`Started ... in N seconds` 가 나오면 성공입니다.

더미값이라 **카카오 로그인만 안 되고 나머지는 전부 동작합니다.**
실제 키를 받으면 `docker-compose.kakao.yml` 의 `KAKAO_CLIENT_ID` 만 바꾸면 됩니다.

`KAKAO_ALLOWED_REDIRECT_URIS` 는 프론트가 보낸 `redirectUri` 를 백엔드가 **글자 하나까지**
비교하는 허용 목록입니다(쉼표 구분). 목록에 없으면 `kakao_redirect_uri_rejected` 로 거부됩니다.
프론트는 '지금 열려 있는 주소 + `kakao-callback.html`' 을 자동으로 만들기 때문에
3000(`serve.js`/`serve.py`)·5500(Live Server) 경우를 모두 넣어 뒀습니다.
다른 포트로 열면 그 주소도 이 목록에 추가해야 합니다.

> 백엔드 팀원분께 전해 주세요 — `docker-compose.local.yml` 에
> `KAKAO_CLIENT_ID` · `KAKAO_ALLOWED_REDIRECT_URIS` · `FRONTEND_BASE_URL` 이 빠져 있습니다.
> 또는 `application.yaml` 에서 `${KAKAO_ALLOWED_REDIRECT_URIS}` → `${KAKAO_ALLOWED_REDIRECT_URIS:}`
> 처럼 기본값을 주면 로컬에서 아무도 안 막힙니다.
> (`kakao.allowed-redirect-uris:` 쪽에는 이미 기본값이 있는데, 그 **안쪽** 값에 없어서 소용이 없습니다.)
> 지금 상태로는 새로 받는 사람마다 똑같이 막힙니다.

### (참고) 에뮬레이션 경고는 원인이 아닙니다

Docker Desktop 에서 app-1 옆에 뜨는
"Image may have poor performance, or fail, if run via emulation" 은
이미지가 amd64 라서 M칩 맥에서 에뮬레이션으로 도는 것입니다. 느리긴 해도 **부팅 실패의 원인은 아니었습니다.**

그래도 켜두면 빨라집니다 — 설정 → General → "Use Rosetta for x86_64/amd64 emulation".

---

## 프론트 ↔ 백엔드 값 맞춤 (2026.08.19 적용 · 실측 완료)

백엔드 머지로 경로와 값이 바뀌어서 **회원가입 마지막 단계가 막혀 있었습니다.** 아래를 서버 기준으로 맞췄고,
브라우저에서 가입 1~4단계를 끝까지 통과시켜 DB 저장까지 확인했습니다.

| 파일 | 고친 것 | 바뀐 값 |
|---|---|---|
| `api.js` | 온보딩 경로 | `/me/onboarding` → `/users/me/onboarding` |
| `api.js` | PATCH 필드명 | `interestCategories` → `interestCategoryCodes` |
| `api.js` | 일반 가입 저장 방식 | POST → **PATCH** (아래 설명) |
| `api.js` | 판정 경로 | `/judgements` → `/judgments` (e 없음) |
| `data.js` | 나이대 '60세 이상' | `AGE_60S` → `AGE_60S_PLUS` |
| `data.js` | 성별 '기타' | `OTHER` → `UNKNOWN` |
| `data.js` | 관심분야 목록 | 서버 `categories` 5개로 교체 |
| `data.js` | `CATEGORIES` | 서버 `categoryId` 순서로 교체 |
| `signup.html` · `interest-edit.html` | 관심분야 버튼 | 4개 → 서버와 같은 5개 |

### 관심분야는 서버 DB 값이 기준입니다

```
1 NUTRITION           영양제·건강기능식품
2 DIET                다이어트·체중관리
3 DISEASE_CARE        질환·증상관리
4 COSMETICS           화장품
5 PROCEDURE_SKINCARE  시술·피부관리
```

예전 `NUTRITION_SUPPLEMENT` · `ETC` 는 서버에 없는 코드라 `400 CATEGORY_001` 이 났습니다.
카테고리가 또 바뀌면 여기서 확인하세요:

```bash
docker exec hackerthon-mysql-1 mysql -udrjudge -plocal_dev_password --default-character-set=utf8mb4 drjudge -e "SELECT id, code, name FROM categories ORDER BY display_order;"
```

### 일반 가입이 POST 를 못 쓰는 이유

`POST /api/users/me/onboarding` 은 **`onboardingToken` 을 필수로 요구**합니다.
그 토큰은 카카오 로그인 응답에서만 나오므로 아이디·비밀번호로 가입한 사람은 가질 수가 없습니다.

```
400 "onboardingToken: onboardingToken은 필수입니다."
```

`PATCH` 는 Authorization 헤더만으로 통과하고 결과도 같아서(`onboardingCompleted: true`)
**토큰이 없으면 PATCH 로 보내도록** 했습니다. 카카오 흐름은 그대로 POST 를 씁니다.

> 백엔드에 요청할 것 — `POST` 의 `onboardingToken` 을 선택값으로 바꿔주면 이 분기가 필요 없어집니다.

### 조용히 실패하던 것 (특히 주의)

PATCH 를 `interestCategories` 라는 틀린 필드명으로 보내면 **서버가 200 을 주면서 관심분야를 저장하지 않습니다.**
오류가 안 나서 못 알아챕니다. 필드명은 POST·PATCH 둘 다 `interestCategoryCodes` 입니다.

### 판정 탭 · 피드 (2026.08.19 추가)

| 파일 | 고친 것 |
|---|---|
| `judge.html` | `api.js` 가 로드되지 않고 있어서 추가 |
| `judge.js` | 최근 판정을 `Store.history`(목업 잔재) 대신 **서버**에서 읽음 |
| `api.js` | 피드 목록 `/feed/posts` → `/feed/posts/me` |
| `api.js` | 피드 정렬(최신·인기)을 화면에서 처리 (서버 `sort` 미지원) |

**`judge.js` 가 통째로 죽던 이유** — `Store` 의 `blank()` 은 `history` 를 만들지 않는데
`judge.js` 가 `me.history.slice(...)` 를 읽어서 `TypeError` 가 났습니다.
`history` 는 어디에서도 채워지지 않는 값이라, 서버의 `GET /api/judgments` 를 쓰도록 바꿨습니다.

**피드는 서버에 '전체 목록' API 가 없습니다.** `GET /api/feed/posts` 는 500 이고
있는 것은 `GET /api/feed/posts/me`(내 게시물)뿐입니다. 지금은 그쪽을 씁니다.

> 백엔드에 요청할 것 — 전체 피드 목록 API(`GET /api/feed/posts`)가 필요합니다.
> 생기면 `api.js` 의 `getFeedPosts` 안 두 줄만 되돌리면 됩니다.

### 관심분야 변경이 안 되던 이유 (2026.08.19)

서버 `GET /api/users/me` 는 **닉네임·포인트·이메일만** 돌려줍니다.
`interestCategories` · `ageGroup` · `gender` 가 응답에 아예 없습니다.

```json
{"nickname":"...","pointBalance":0,"email":"..."}
```

그런데 `api.js` 의 `getMe` 가 없는 값을 **빈 배열로 만들어** 넘겼고,
`syncProfile` 이 그걸로 화면에 저장돼 있던 관심분야를 덮어썼습니다.

그래서 관심분야 변경 화면이 **매번 아무것도 선택 안 된 상태로 열렸고**,
거기서 하나를 고르고 저장하면 기존 관심분야가 **전부 사라지고 그 하나로 바뀌었습니다.**
(PATCH 는 목록을 통째로 교체합니다.)

값이 안 오면 `undefined` 로 두어 '건드리지 않음'이 되도록 고쳤습니다.
확인 — 기존 2개가 유지된 채 1개를 추가하면 DB 에 3개가 남습니다.

> 백엔드에 요청할 것 — `GET /api/users/me` 응답에
> `interestCategories` · `ageGroup` · `gender` 를 넣어 주세요.
> 지금은 다른 기기에서 바꾼 관심분야를 이 기기가 알 방법이 없습니다.

### 확인된 결과

```
POST  /api/auth/signup             201
POST  /api/auth/login              200
PATCH /api/users/me/onboarding     200
GET   /api/users/me                200
GET   /api/users/me/points/history 200
GET   /api/judgments               200
GET   /api/feed/posts/me           200
GET   /api/briefings/today         200
```

DB 저장 확인 — 예전에 400 이 나던 조합(60세 이상 · 성별 기타 · 신규 카테고리)으로 테스트:

```
users:          age_group=AGE_60S_PLUS  gender=UNKNOWN
user_interests: NUTRITION, PROCEDURE_SKINCARE
```

---

## 지금 어디까지 되는가 (백엔드 전달 기준)

| 기능 | 지금 상태 | 확인 방법 |
|---|---|---|
| 회원가입 · 로그인 · 토큰 · 탈퇴 | **된다** | 가입 → 로그인 → 홈 진입 |
| 카카오 로그인 | **된다** (REST API 키 필요) | 시작 화면 → 카카오로 시작하기 |
| 온보딩 저장 · 수정 | **된다** | 가입 3·4단계 → 마이페이지 관심분야 변경 |
| 브리핑 · 피드 목록 · 좋아요 · 포인트 내역 | 서버는 됨, **내용이 비어 있을 수 있음** | 화면이 '아직 없어요'로 뜨면 정상 |
| **판정** | **아직 안 됨** — AI 서비스 미연결 | 아래 참고 |
| 공유 링크 · 피드 게시 | 판정이 있어야 가능 | 판정이 열린 뒤에 |

### 판정은 왜 실패하나

백엔드는 떠 있지만 **판정을 실제로 계산하는 AI 서비스(8000)가 아직 연결 전**입니다.
그래서 판정을 요청하면 접수(202)까지는 되고, 결과를 조회하면 `status: FAILED` 로 옵니다.

**앱은 이걸 오류로 보지 않고 '추출 실패' 안내 화면으로 넘어갑니다.** 지금은 그게 정상 동작이에요.
AI 가 붙으면 프론트는 고칠 게 없습니다 — 같은 응답의 `status` 만 `DONE` 으로 바뀝니다.

판정에서 파생되는 **공유 링크 발급, 피드 게시**도 판정 결과가 있어야 하므로
그때까지는 화면만 확인할 수 있습니다.

### 오늘 확인해 볼 순서

1. `docker compose -f docker-compose.local.yml pull` — **다시 받으세요.** 백엔드가 업데이트됐습니다
2. `docker compose -f docker-compose.local.yml up -d`
3. http://localhost:8080/swagger-ui.html 가 뜨는지
4. `node serve.js` → http://localhost:3000/start.html
5. 회원가입 → 로그인 → 홈 → 마이페이지까지
6. 판정은 '추출 실패'가 뜨면 **서버까지 잘 갔다는 뜻**입니다

---

## Netlify 배포본 (https://drjudge.netlify.app)

### 백엔드 주소 (2026.08.19 연결 완료)

```
https://13-124-27-143.sslip.io
```

`dr.judge/api.js` 의 `SERVER_URL` 에 채워 넣었습니다. **주소가 바뀌면 이 한 줄만 고치면 됩니다.**
백엔드에 `drjudge.netlify.app` 과 `localhost:3000` CORS 가 이미 열려 있어 프록시 없이 바로 붙습니다.

> IP 기반 임시 도메인(sslip.io)이라 서버 IP 가 바뀌면 주소도 바뀝니다.
> 그때는 `api.js` 의 `SERVER_URL` 한 줄만 교체하세요.

### 어느 백엔드에 붙는지 — 규칙

| 어떻게 열었나 | 붙는 백엔드 |
|---|---|
| `python3 serve.py` → localhost:3000 | **내 컴퓨터** 백엔드 (예전 그대로) |
| localhost:3000/start.html**?api=server** | **배포 서버** |
| Netlify (drjudge.netlify.app) | **배포 서버** |

로컬 개발은 아무것도 안 바뀝니다. 배포 서버에 붙여 보고 싶을 때만 `?api=server` 를 붙이세요.

### 확인한 것

배포 서버는 로컬 이미지와 **엔드포인트가 완전히 동일**합니다. 실제로 돌려봤습니다.

```
POST  /api/auth/signup           201
POST  /api/auth/login            200
PATCH /api/users/me/onboarding   200   (관심분야·연령대·성별 저장됨)
      잘못된 카테고리 코드        400   (검증도 동일)
```

`?api=server` 로 열어 화면에서 가입까지 성공했습니다(배포 DB 에 저장 확인).

> `_redirects` 의 프록시 규칙은 주석으로 남겨 뒀습니다.
> 나중에 CORS 가 막히면 그걸 풀어서 쓰면 됩니다.

### 예전에 가입이 안 되던 이유 (2026.08.19)

배포본 `api.js` 가 이 주소를 부르고 있었습니다.

```
http://drjudge.netlify.app:8080/api/auth/signup
```

세 겹으로 막힙니다 — ① 그런 서버 없음 ② https 페이지에서 http 호출은 브라우저가 차단
③ 백엔드가 `127.0.0.1:8080` 에 묶여 있어 그 맥 밖에서는 접근 불가(LAN IP 로도 연결 거부).
**주소만 고쳐서는 안 되고 백엔드가 공개 https 로 떠 있어야 합니다.**

### 이미지가 안 나오던 이유

배포할 때 `assets` 폴더가 풀려서 파일들이 **루트에 올라갔습니다.**

```
/assets/character.png   404
/character.png          200  ← 여기 있었음
```

`_redirects` 에 보정 규칙을 넣어 뒀습니다.

```
/assets/*  /:splat  200
```

다음에 `dr.judge` 폴더를 **폴더째** 올리면 구조가 정상으로 돌아오고,
그때는 실제 파일이 우선이라 이 규칙은 자동으로 무시됩니다.

### favicon 404

29개 HTML 어디에도 favicon 선언이 없어서 페이지마다 `/favicon.ico` 404 가 났습니다.
전부에 `<link rel="icon" href="/assets/app-icon.png">` 를 넣었습니다.

### 배포할 때 주의

`dr.judge` 폴더를 **폴더째** 올리세요. 파일만 골라 올리면 `assets` 가 또 풀립니다.

---

## 미리 알아둘 것 세 가지

### 0. CORS 로 막히면 — 로컬 서버로 우회 (백엔드 수정 없이 해결)

콘솔에 `blocked by CORS policy` 가 뜨면, `hackerthon` 폴더에서 **둘 중 하나**를 실행하세요.

```bash
python3 serve.py      # 맥에 기본으로 있음 — 설치 필요 없음
node serve.js         # Node 가 깔려 있다면
```

그다음 **http://localhost:3000/start.html** 로 여세요.

두 파일은 하는 일이 완전히 같습니다. 화면과 `/api` 를 **같은 주소에서** 내주기 때문에
브라우저가 CORS 검사를 아예 하지 않습니다. 백엔드 설정을 건드릴 필요가 없어요.

- 설치할 패키지 없음
- 백엔드 주소가 다르면 `API=http://127.0.0.1:9090 python3 serve.py`
- 포트를 바꾸려면 `PORT=4000 python3 serve.py`
- 끄려면 Ctrl+C

Live Server(5500) 대신 쓰는 것이고, 파일을 고치면 새로고침으로 바로 반영됩니다.

> `zsh: command not found: node` 가 뜨면 Node 가 없는 것입니다. `serve.py` 를 쓰세요.
> 맥에는 python3 가 기본으로 들어 있습니다. (`python3 --version` 으로 확인)

> 정식 해결은 백엔드에서 출처를 열어주는 것입니다(아래 1번).
> `serve.js` 는 그때까지 막히지 않고 개발하기 위한 우회로입니다.

---

### 1. 주소를 섞어 쓰면 CORS 로 막힙니다

`http://localhost:5500` 과 `http://127.0.0.1:5500` 은 브라우저가 **다른 출처**로 봅니다.
백엔드가 둘 중 하나만 허용하고 있으면 다른 쪽에서는 요청이 전부 실패해요.
**항상 같은 형태로** 여세요 (예: 계속 `localhost` 로만).

콘솔에 `blocked by CORS policy` 가 보이면 백엔드에서 아래를 열어달라고 하면 됩니다.

```
http://localhost:5500
http://127.0.0.1:5500
```

### 2. 판정은 AI 서비스가 따로 떠 있어야 합니다

compose 파일 주석에 있는 내용입니다.

> AI_SERVICE_BASE_URL 이 실제로 떠 있지 않아도 앱은 정상 부팅된다 —
> 판정(Judgment) 요청을 실제로 보낼 때만 FAILED 로 처리된다

즉 Python 서비스(8000)를 안 띄우면 판정 요청은 접수(202)까지는 되지만
조회했을 때 `status: FAILED` 로 옵니다. 이때 화면은 **추출 실패 안내**로 넘어가요.
회원가입·로그인·이력 조회는 AI 서비스 없이도 정상 동작합니다.

### 3. 포트가 127.0.0.1 에 묶여 있습니다

```yaml
ports:
  - "127.0.0.1:8080:8080"
```

내 컴퓨터에서만 접근됩니다. 같은 와이파이의 휴대폰으로 테스트하려면 이 줄을
`"8080:8080"` 으로 바꿔야 해요. (그러면 같은 네트워크에 서버가 열리니 주의)

---

## 다음 단계

Swagger 가 뜨면 아래 주소에서 전체 API 명세를 JSON 으로 받을 수 있습니다.

```
http://localhost:8080/v3/api-docs
```

이 내용을 `dr.judge/api-docs.json` 으로 저장해 두면, 남은 API(중복 확인, 프로필 저장,
피드, 포인트 등)를 한 번에 연결할 수 있습니다.
